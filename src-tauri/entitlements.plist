<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Microphone + system audio capture. Note: NO network entitlements. -->
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <key>com.apple.security.device.microphone</key>
    <true/>
    <!-- App sandbox is ON, network.client is deliberately ABSENT.
         macOS itself will kill any outbound connection Sotto attempts.
         That is the Airlock, enforced below the process. -->
    <key>com.apple.security.app-sandbox</key>
    <true/>
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
    <!-- Read-only local calendar (EventKit) for auto-titling and briefs. -->
    <key>com.apple.security.personal-information.calendars</key>
    <true/>
    <key>com.apple.security.assets.movies.read-write</key>
    <true/>
</dict>
</plist>
